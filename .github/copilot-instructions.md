Never use heredocs or multi-line terminal pastes. If you need to create a file, use editor edits / apply patch. If you must run something, run a short single-line command only.
No heredocs or multi-line pastes are allowed in any context: use runSubagent to handle complex file modification tasks.
